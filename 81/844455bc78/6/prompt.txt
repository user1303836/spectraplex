IMPORTANT: After completing the task below, you MUST output a JSON object in a ```json code fence at the very end of your response. Do NOT forget this — the workflow fails without it.

Coordinate the next Spectraplex work packet lifecycle.

Read before deciding:
- CLAUDE.md
- design/SPECTRAPLEX_STRATEGY_AND_EXECUTION_PLAN.md
- design/PHASED_IMPLEMENTATION_PLAN.md
- the current repository state
- relevant open pull requests and packet branches

Preflight:
All preflight checks pass. Worktree is clean, on main at 484a3f3 (up-to-date with origin), all required tools and GitHub auth are available with full repo permissions, and both design docs exist. Ready for autonomous packet work.

Workflow input:
- goal: (not provided)
- startAtPacket: (not provided)
- stopAfterPacket: (not provided)
- maxMergedPackets: (not provided)

Previous coordination:
P1-W3 merged as PR #107. 262 tests pass on main at 1e50a92. No in-flight packets or open PRs. Selecting P1-W4 — the dual-write compatibility layer that bridges V1 wallet ingestion to V2 canonical storage. This is the critical compatibility packet: every write to V1 tables (transactions, indexer_checkpoints) must also populate V2 tables (raw_transactions, target_matches, checkpoints) with an auto-created wallet IndexTarget. The dual-write lives in the repository layer per RFC Section 5.2. V2 writes are best-effort during early transition. Dependencies P1-W2 (V2 tables) and P1-W3 (V2 repo methods) are both merged.

Previous active packet runtime:
- stage: fix_review_feedback
- implementation: Added dual-write compatibility layer: V1→V2 conversion functions (chain-to-network mapping, transaction/checkpoint conversion, target match construction), ensure_wallet_target idempotent shim, best-effort V2 writes in CLI and API ingestion paths, 32 new unit tests, and tracked handoff doc. All 294 tests pass with zero regressions. No V1 signatures changed.
- validation: P1-W4 passes all validation checks. cargo fmt, clippy, and all 294 tests (262 baseline + 32 new) are clean with zero regressions. No V1 signatures, API handlers, or CLI flags were modified. Chain-to-network mapping matches Rollout Plan §2.4 exactly. V2 RawTransaction correctly strips user_id/wallet_address. Checkpoint JSONB cursors match P0-W3 §5-6. All V2 write failures are caught and logged without affecting the V1 path. ensure_wallet_target uses an idempotent lookup-or-create pattern. The handoff artifact is present and in sync with the implementation.
- PR state: Created PR #108 for P1-W4. Pushed branch to origin and opened PR against main with full description covering dual-write layer (5 files, 947 insertions), validation results (294 tests, clean fmt/clippy), and handoff artifact. PR is open and ready for review.
- local review: The dual-write compatibility layer is well-structured with clean separation, thorough unit tests (32 new, 294 total passing), and correct best-effort V2 error handling. All CI checks pass. One blocking issue: the Hyperliquid checkpoint cursor uses 'last_timestamp_ms' (with ms conversion) instead of the required 'last_timestamp' key — the ms conversion is explicitly post-P1-W4 per the review spec. Five non-blocking issues noted including a missing ON CONFLICT DO NOTHING in the upstream P1-W3 index_target INSERT, match_reason traceability, missing DB integration tests, defensive coding suggestions, and streaming path coverage.
- remote review: 1 blocking issue: Hyperliquid checkpoint cursor key must be 'last_timestamp' (raw seconds) not 'last_timestamp_ms' (ts*1000) — the ms conversion is post-P1-W4. All other review focus areas pass: conversion correctness, best-effort V2 writes, chain-to-network mapping, V1 method preservation, target match creation, and minimal API/CLI wiring. CI fully green.
- merge: No merge output yet.

Previous merge result:
No prior merge result.

Merged packets so far:
1. Phase 0: Lock The V2 Architecture P0-W1: V2 Architecture RFC
2. Phase 0: Lock The V2 Architecture P0-W3: Network Model Spec
3. Phase 0: Lock The V2 Architecture P0-W4: Rollout And Compatibility Plan
4. Phase 1: Canonical Bronze And Control Plane P1-W1: Introduce V2 Core Types
5. Phase 1: Canonical Bronze And Control Plane P1-W2: Add New Bronze And Control-Plane Tables
6. Phase 1: Canonical Bronze And Control Plane P1-W3: Repository Support For Canonical Raw Data

Rules:
1. Continue exactly one in-flight packet until it is merged. In-flight means an unmerged packet already selected by this workflow, an open PR for a packet branch, or local branch work that clearly maps to a packet.
2. Only select a new packet when there is no in-flight packet left to finish.
3. If multiple conflicting in-flight packets exist, return state="blocked" and explain why.
4. Select the earliest incomplete packet allowed by dependencies in design/PHASED_IMPLEMENTATION_PLAN.md and still incomplete in the current repository and tracked docs.
5. Respect phase order. Do not skip prerequisites unless the phased plan explicitly allows it.
6. Keep wallet indexing first-class, but do not reinforce wallet-only assumptions in shared abstractions.
7. If a packet's primary deliverables live under gitignored paths such as design/, require at least one tracked review artifact under docs/ and list it in trackedReviewArtifacts.
8. Remote review happens as a PR comment, not a formal GitHub approval. Do not block on missing formal approval.
9. Admin or force merge is allowed only after validation, local review, and the remote review comment all say the remaining blockers are cleared.
10. If stopAfterPacket is provided and that packet is already merged, return state="complete".
11. If maxMergedPackets is provided and the limit has been reached, return state="complete".
12. If a prior merge attempt failed for an external reason that the workflow cannot safely resolve, return state="blocked" instead of looping forever.

Return JSON with:
- workflowComplete: true only if the workflow should stop successfully
- state: "active", "complete", or "blocked"
- phase: string, or "" if complete or blocked
- workPacket: string, or "" if complete or blocked
- objective: string, or "" if complete or blocked
- branchName: string, or "" if complete or blocked
- prNumber: number, or 0 if none
- prUrl: string
- prTitle: string
- prBody: string
- summary: concise coordination summary
- repoTouchpoints: array
- implementationSteps: array
- verificationSteps: array
- reviewFocus: array
- trackedReviewArtifacts: array
- blockingIssues: array
- warnings: array
- nextAction: string


**REQUIRED OUTPUT** — You MUST end your response with a JSON object in a code fence matching this schema:
```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {
    "workflowComplete": {
      "type": "boolean"
    },
    "state": {
      "type": "string",
      "enum": [
        "active",
        "complete",
        "blocked"
      ]
    },
    "phase": {
      "type": "string"
    },
    "workPacket": {
      "type": "string"
    },
    "objective": {
      "type": "string"
    },
    "branchName": {
      "type": "string"
    },
    "prNumber": {
      "type": "number"
    },
    "prUrl": {
      "type": "string"
    },
    "prTitle": {
      "type": "string"
    },
    "prBody": {
      "type": "string"
    },
    "summary": {
      "type": "string"
    },
    "repoTouchpoints": {
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "implementationSteps": {
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "verificationSteps": {
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "reviewFocus": {
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "trackedReviewArtifacts": {
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "blockingIssues": {
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "warnings": {
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "nextAction": {
      "type": "string"
    }
  },
  "required": [
    "workflowComplete",
    "state",
    "phase",
    "workPacket",
    "objective",
    "branchName",
    "prNumber",
    "prUrl",
    "prTitle",
    "prBody",
    "summary",
    "repoTouchpoints",
    "implementationSteps",
    "verificationSteps",
    "reviewFocus",
    "trackedReviewArtifacts",
    "blockingIssues",
    "warnings",
    "nextAction"
  ],
  "additionalProperties": false
}
```
Output the JSON at the END of your response. The workflow will fail without it.