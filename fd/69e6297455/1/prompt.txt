IMPORTANT: After completing the task below, you MUST output a JSON object in a ```json code fence at the very end of your response. Do NOT forget this — the workflow fails without it.

Implement the current Spectraplex packet.

Follow:
- CLAUDE.md
- design/SPECTRAPLEX_STRATEGY_AND_EXECUTION_PLAN.md
- design/PHASED_IMPLEMENTATION_PLAN.md

Current coordination:
- phase: Phase 3: Expand Silver Into Reusable Datasets
- work packet: P3-W4: Hyperliquid reusable datasets
- objective: Add first-class Silver datasets for Hyperliquid fills, funding payments, and position changes, with materializers and repository methods following the P3-W2/P3-W3 Silver dataset pattern.
- branch: p3-w4-hyperliquid-datasets
- stage: new packet implementation
- existing PR: 

Repo touchpoints:
- core/src/materializer.rs
- adapters/src/hyperliquid_parser.rs
- adapters/src/v2_repo.rs
- adapters/src/lib.rs
- migrations/

Implementation steps:
1. Define HlFill, HlFundingPayment, and HlPositionChange Silver structs in core/src/materializer.rs following DecodedEvent/TokenTransfer pattern (no wallet-specific fields)
2. Add hl_fills, hl_funding_payments, and hl_position_changes migration with IF NOT EXISTS guards and nullable FKs
3. Implement extraction functions in adapters/src/hyperliquid_parser.rs for each dataset from Bronze raw_transactions metadata
4. Add HlFillMaterializer, HlFundingPaymentMaterializer, and HlPositionChangeMaterializer with unique parser hashes
5. Add repository bulk insert and query methods (save/get) in adapters/src/v2_repo.rs
6. Export new materializers in adapters/src/lib.rs and add to global hash uniqueness test
7. Add comprehensive unit tests for extraction, serialization, and repository query builders

Verification expectations:
1. cargo fmt --all --check
2. cargo clippy --workspace --all-targets -- -D warnings
3. cargo test --workspace (all tests pass, 0 failures)
4. Verify all materializer parser hashes remain globally distinct (should reach 13 total)
5. Verify Silver structs have no wallet_address or user_id fields
6. Verify migrations use IF NOT EXISTS and nullable FKs
7. Verify existing evm_logs, blocks, and hl_fills extension tables are untouched

Tracked review artifacts required in the PR:
- No tracked review artifact required.

Outstanding validation or review feedback:
No outstanding validation or review feedback.

Requirements:
1. If this is a new packet implementation, create or reset the local branch p3-w4-hyperliquid-datasets from the latest main branch state.
2. If this is a validation or review fix iteration, stay on the existing branch and address the outstanding blockers first.
3. Make only the changes needed for P3-W4: Hyperliquid reusable datasets.
4. If the packet's authoritative outputs live in gitignored files, create or update the tracked review artifacts listed above so the PR is actually reviewable on GitHub.
5. Run the relevant verification commands before finishing.
6. Create a local commit if changes were made and the packet is in a good local state.
7. Do not push, do not create or update the PR, and do not merge.

Return JSON with:
- phase: "Phase 3: Expand Silver Into Reusable Datasets"
- workPacket: "P3-W4: Hyperliquid reusable datasets"
- branchName: "p3-w4-hyperliquid-datasets"
- commitSha: the local commit SHA, or "" if no commit was created
- summary: concise summary of what changed
- filesChanged: array of changed files
- testsRun: array of commands or checks you ran
- docsUpdated: array of docs updated
- followUps: array of remaining follow-up items


**REQUIRED OUTPUT** — You MUST end your response with a JSON object in a code fence matching this schema:
```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {
    "phase": {
      "type": "string"
    },
    "workPacket": {
      "type": "string"
    },
    "branchName": {
      "type": "string"
    },
    "commitSha": {
      "type": "string"
    },
    "summary": {
      "type": "string"
    },
    "filesChanged": {
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "testsRun": {
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "docsUpdated": {
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "followUps": {
      "type": "array",
      "items": {
        "type": "string"
      }
    }
  },
  "required": [
    "phase",
    "workPacket",
    "branchName",
    "commitSha",
    "summary",
    "filesChanged",
    "testsRun",
    "docsUpdated",
    "followUps"
  ],
  "additionalProperties": false
}
```
Output the JSON at the END of your response. The workflow will fail without it.