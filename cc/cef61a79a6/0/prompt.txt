Spin up 4 agents to work on the high priority issues in the repo. 2 should focus on implementation, 2 should RIGOROUSLY scrutinize the work and do reviews. The implementation agents should work on worktrees, and make PRs when ready. the reviewers should do PR reviews, with a deep understanding of the purpose of the project at a high level. PRs can only be merged when all review comments are merged. All agents should first deep dive the repo to gain deep understanding of the code, and critical execution paths, and the purpose of the repo

---

<task-notification>
<task-id>abeb599132edb1ce2</task-id>
<tool-use-id>toolu_01UmP986ePtm7QVPWc2fHbVG</tool-use-id>
<output-file>REDACTED.output</output-file>
<status>completed</status>
<summary>Agent "Deep dive for security PR review" completed</summary>
<result>Perfect. Now I have a comprehensive understanding. Let me create a detailed summary:

---

## COMPREHENSIVE CODE REVIEW DEEP DIVE - SPECTRAPLEX

Based on thorough analysis of the Spectraplex codebase, here is my expert assessment:

### PROJECT OVERVIEW & ARCHITECTURE

**Purpose**: Spectraplex is a Rust-based multi-chain blockchain indexing and normalization system supporting Solana, EVM, and Hyperliquid. It ingests raw chain data into Bronze layer, normalizes to Silver/Gold datasets, and exposes both legacy wallet APIs and modern target-/dataset-centric endpoints.

**Four Workspace Crates**:
- `core/`: Models (Transaction, LedgerEntry, Chain enums), config loading, V2 target/network types
- `adapters/`: Chain adapters (Solana RPC/gRPC, EVM eth_getLogs, Hyperliquid REST), parsers, PostgreSQL persistence layer
- `cli/`: Command-line interface for init-db, ingest, normalize, V2 target management
- `api/`: Axum HTTP server with ingestion jobs, normalization, queries, exports, analytics

**Strategic Direction** (Critical): The system is evolving from wallet-centric toward target-centric indexing, supporting wallets, contracts, programs, markets, pools, and protocols as first-class targets. This is important for PR reviews: avoid wallet-only assumptions in core abstractions.

---

### SECURITY SURFACE & PATTERNS

#### STRENGTHS:

1. **Constant-Time API Key Comparison**
   - Uses `subtle::ConstantTimeEq` in `require_auth()` (line 367)
   - Prevents timing-based API key leakage

2. **SSRF Protection**
   - Comprehensive IP blocking in `is_private_ip()` (line 520-536):
     - Blocks IPv4: loopback, private ranges, link-local, broadcast, unspecified
     - Blocks IPv6: loopback, unspecified
     - Blocks hostname: "localhost"
   - Used in `validate_callback_url()` for webhook/callback URLs
   - Applied to both job callback_urls and sink webhook configs

3. **Input Validation**
   - `validate_wallet()`: Length (0-128), charset (alphanumeric + 'x' only)
   - `validate_dataset_name()`: Length (1-64), charset (alphanumeric + underscore only)
   - `validate_date_range()`: Ensures from <= to
   - Whitelist validation: EXPORTABLE_DATASETS and QUERYABLE_DATASETS are explicit lists

4. **Address Normalization**
   - `normalize_evm_address()`: Lowercase (case-insensitive comparison safe)
   - `normalize_solana_address()`: Passthrough (case-sensitive by design)
   - Applied consistently before storage (line 1472-1475)

5. **Path Traversal Protection**
   - `validate_sink_config()` rejects file_path containing ".." (line 576-580)
   - Prevents directory traversal via sink output

6. **SQL Injection Prevention**
   - Uses parameterized queries throughout (`sqlx::query` with `.bind()`)
   - `build_transaction_insert()` uses placeholders (e.g., `$1`, `$2`)
   - No string concatenation for untrusted input in WHERE/INSERT

7. **Export Download Headers**
   - Proper Content-Disposition header with attachment (line 2795-2808)
   - Content-Type set from whitelist
   - Job ID validation before serving data (line 2786-2787)

#### AREAS TO WATCH:

1. **CSV Escaping Simplicity**
   - `csv_escape()` (line 1130-1136) is minimal but adequate (double-quote escaping)
   - For financial data, this is acceptable but verify CSV parsers handle edge cases

2. **Wallet Allowed-List**
   - `check_wallet_allowed()` (line 706-712) does case-insensitive matching for allowed wallets
   - Good: prevents case-based bypasses
   - Ensure the HashSet is correctly populated from config

3. **Export Job Memory Storage**
   - Export data stored in-memory (`ExportJobEntry::data`) for up to 1 hour (JOB_TTL_SECS = 3600)
   - With MAX_CONCURRENT_JOBS = 10, memory is bounded but worth monitoring for large exports
   - Proper cleanup via `prune_stale_export_jobs()` (line 159-162)

4. **Error Messages**
   - Generally safe: avoid leaking internal paths or query details
   - "Internal server error" is logged but not returned to client
   - Good pattern throughout

---

### CRITICAL EXECUTION PATHS & CODE QUALITY

#### Ingest Pipeline:
1. **API Entry** (`trigger_ingest()`, line 715): Validates wallet, checks allowed list, validates callback URL, acquires semaphore permit
2. **Chain Adapter** (`SolanaAdapter`, `EvmAdapter`, `HyperliquidAdapter`): Fetches transactions from RPC/gRPC
3. **Storage** (`repo.save_transactions()`): Batch inserts with ON CONFLICT handling (line 149)
4. **Callback** (`fire_callback()`): HTTP POST to validated URL (line 504-518)

**Quality**: Well-structured async job system. Semaphore prevents resource exhaustion. Transaction batching at 500 records (line 105).

#### Normalization Pipeline:
1. **Parser Selection** (line 2608-2700): Match on dataset name (whitelist enforced)
2. **Chain-Specific Logic**:
   - EVM: Parses ERC-20 Transfer logs, native value transfers, gas fees
   - Solana: Extracts SOL changes, fee separation, SPL token balance changes
   - Hyperliquid: Fills, funding entries, position changes
3. **Ledger Entry Creation**: Deterministic IDs using `deterministic_id(tx.id, entry_index)`

**Quality**: Comprehensive parsing logic. Separate files for chain logic reduce coupling. No obvious gaps.

#### Export Job Execution:
1. **Job Creation** (`create_export_job()`, line 2314): Validates dataset, format, date range, sink config
2. **Async Spawn** (line 2385): Runs export outside request scope
3. **Data Serialization** (`run_export_job()`, line 2526): JSONL or CSV based on format
4. **Sink Delivery** (line 2446-2490): Outside the RwLock to avoid blocking reads
5. **Cleanup** (line 2494-2495): Sets finished_at for TTL-based removal

**Quality**: Lock discipline is solid. Sink delivery is properly decoupled from state mutation. Separate locks for short vs. long operations.

---

### STRONG AREAS

1. **Test Coverage**: 547 tests across the workspace (403 in adapters, 144 in core, ~245 in API)
   - Coverage includes parsers, repository layer, config loading, V2 enums, API handlers
   - Tests verify wallet validation, dataset name validation, address normalization, error cases

2. **Async/Concurrency**: Proper use of Tokio, RwLock for read-heavy state, Semaphores for resource limits
   - Stream management with CancellationToken (line 1301-1360)
   - Job management with job_semaphore (MAX_CONCURRENT_JOBS = 10)

3. **Error Handling**: Consistent AppError enum with proper HTTP status codes
   - 400 Bad Request for validation failures
   - 401 Unauthorized for auth failures
   - 403 Forbidden for wallet allowlist violations
   - 404 Not Found for missing resources
   - 409 Conflict for in-progress jobs
   - 500 Internal Server Error (logged, not exposed)

4. **Configuration**: Flexible loading from defaults → toml → env (Figment library)
   - Proper handling of optional fields (grpc_url, grpc_token, allowed_wallets)

5. **V2 Architecture**: Target/Network/Dataset abstraction is clean
   - Supports multiple target kinds (wallet, contract, program, market, pool, protocol)
   - Chainless datasets via networks table
   - Extensible for future target types

---

### WEAK AREAS / GAPS

1. **Parser Coverage Asymmetry**:
   - EVM parser handles ERC-20 transfers + native value + gas fees
   - Solana parser handles SOL + SPL tokens + fees
   - Hyperliquid parser handles fills + funding + position changes
   - **Gaps**: Multi-hop swaps, AMM interactions, complex contract calls may not normalize correctly

2. **No Rate Limiting**: API has no built-in rate limiting or per-wallet quota enforcement
   - Relies on semaphore permits but not per-user
   - Consider adding if this becomes a public API

3. **Limited Testing for Large Exports**: Tests cover small exports but not stress testing
   - Large JSONL/CSV generation in memory
   - Should verify memory usage doesn't explode for 100k+ records

4. **Solana Determinism**: Parser uses metadata extraction (slot, block) but relies on RPC
   - If RPC changes schema, parsing breaks silently (e.g., missing "slot" field)
   - No version pinning or fallback logic

5. **Webhook Retries**: `fire_callback()` has no retry logic or backoff
   - Single attempt with 10-second timeout
   - Transient network failures = lost callbacks

6. **CSV Header Injection**: CSV escape prevents value injection but header row is hardcoded
   - If future CSVs need dynamic headers, ensure they're validated

---

### WHAT TO LOOK FOR IN PR REVIEWS

1. **Wallet vs. Target-Centric**: 
   - PRs adding new parsers/queries should generalize across target types, not just wallets
   - Check if `allowed_wallets` config is extended to other target types

2. **Input Validation**:
   - Any new query parameter must be validated (length, charset, range)
   - New endpoints must validate path params (wallet, dataset name, etc.)

3. **SQL Injection**:
   - All database queries must use parameterized queries
   - Reject PRs using string concatenation for dynamic WHERE/INSERT clauses

4. **SSRF in New Endpoints**:
   - Any URL/callback parameter must go through `validate_callback_url()`
   - Check that private IP check includes the specific scenario (e.g., metadata server bypass via IPv6)

5. **Error Message Leakage**:
   - Verify error messages don't expose internal paths, query details, or stack traces to clients
   - Use `AppError::internal()` for unexpected errors

6. **Export Format Safety**:
   - New export formats (CSV, JSON variants) must properly escape/encode data
   - Verify headers are not user-controlled

7. **Resource Limits**:
   - Long-running operations (exports, streams) should respect MAX_CONCURRENT limits
   - Check for unbounded memory allocation (e.g., loading entire dataset into Vec)

8. **Address Normalization**:
   - Any new chain/target must define normalization rules
   - Ensure stored addresses are normalized consistently
   - Comparison queries must normalize both sides

9. **Tests Completeness**:
   - Any new parser must have tests for: happy path, empty/null fields, malformed data, edge cases
   - Integration tests should verify round-trip (raw → silver → export)

10. **Dependency Changes**:
    - Verify new dependencies don't introduce security issues
    - Check for version pins on critical deps (sqlx, axum, tokio)

---

### SCORING SUMMARY

**Architecture & Design**: 8/10 - Solid layered design, clear separation of concerns, good async patterns. Minor gaps in parser coverage.

**Security**: 8/10 - SSRF protection, input validation, SQL injection prevention all solid. No rate limiting is a gap. Webhook retry logic missing.

**Code Quality**: 8/10 - Good error handling, consistent patterns, decent test coverage. Parser logic is sometimes complex; CSV escaping could be more robust.

**Maintainability**: 8/10 - Clear module organization, reasonable function sizes, good use of Rust's type system. Some large files (api/src/main.rs at 7596 lines).

**Testing**: 7/10 - 547 tests is good baseline. Gaps in large export testing, Solana determinism verification, and e2e integration tests.

---

I am now ready to perform detailed reviews of pull requests with context on the codebase strengths, security-sensitive areas, and quality standards. I understand the strategic direction toward target-centric indexing and will watch for PRs that either align with or inadvertently deepen wallet-only assumptions.</result>
<usage><total_tokens>80324</total_tokens><tool_uses>39</tool_uses><duration_ms>154152</duration_ms></usage>
</task-notification>
Full transcript available at: REDACTED.output

---

<task-notification>
<task-id>ad565922447ea1267</task-id>
<tool-use-id>REDACTED</tool-use-id>
<output-file>REDACTED.output</output-file>
<status>completed</status>
<summary>Agent "Deep dive for core bugs PR review" completed</summary>
<result>Perfect! Now I have a comprehensive understanding of the Spectraplex codebase. Let me create a detailed summary of my findings.

---

## COMPREHENSIVE DEEP DIVE SUMMARY: SPECTRAPLEX CODEBASE

I have thoroughly explored the Spectraplex codebase. Here is my expert-level understanding:

### **PROJECT PURPOSE AND DIRECTION**

Spectraplex is a multi-chain blockchain indexing and normalization system built in Rust with four workspace crates. It started wallet-first for tax/forensics but is evolving toward a **target-centric indexing platform** where wallets are just one target type among eight: wallet, contract, program, account, topic_filter, market, pool, and protocol.

**Key directive from CLAUDE.md**: Avoid deepening wallet-only assumptions; prefer canonical raw data, reusable datasets, and downstream materializations. Wallets must remain first-class but not the organizing principle.

---

### **ARCHITECTURE OVERVIEW**

#### **Four Workspace Crates**

1. **`core/`** - Models, config, traits
   - `models.rs`: `Transaction` (Bronze), `LedgerEntry` (Silver), `Chain` enum, `EntryType`, `ChainIngestor` trait
   - `config.rs`: `AppConfig` with `allowed_wallets_set()` parsing CSV to HashSet with case normalization
   - `v2.rs`: Target-centric types (`IndexTarget`, `TargetKind`, `TargetMode`, `Network`, `ChainFamily`)
   - `materializer.rs`: `Materializer` trait for parser versioning, `DatasetName` enum, sink configs

2. **`adapters/`** - Chain adapters, parsers, repository
   - `solana.rs`: RPC ChainIngestor + V2 Connector for backfill
   - `solana_grpc.rs`: Yellowstone gRPC streaming with automatic reconnection and checkpointing
   - `solana_parser.rs`: Ledger parsing, token transfers, native balance deltas, decoded events
   - `evm_parser.rs`: ERC-20 transfers, native ETH, gas fees
   - `hyperliquid_parser.rs`: Fills, funding, positions
   - `repo.rs`: PostgreSQL persistence layer with batch inserts

3. **`cli/`** - Entry points
   - `ingest`: Fetches from RPC/gRPC, outputs JSONL
   - `normalize`: Parses JSONL to ledger entries
   - `register-target`: V2 target registration
   - `list-targets`, `list-networks`

4. **`api/`** - Axum HTTP server
   - Legacy wallet endpoints: `/v1/transactions/{wallet}`, `/v1/ledger/{wallet}`, `/v1/export/{wallet}`
   - V2 target/dataset endpoints: `/v1/targets`, `/v1/datasets`, `/v1/networks`
   - Analytics endpoints for Hyperliquid and protocol
   - Job control: `/v1/ingest`, `/v1/normalize`, `/v1/jobs/{id}`

#### **Data Layers**

- **Bronze**: Raw immutable transactions (V1: `transactions` table; V2: `raw_transactions` without wallet/user_id)
- **Silver**: Normalized financial entries (ledger entries, token transfers, native balance deltas, decoded events)
- **Gold**: Materialized datasets (wallet_ledger, balance_history, hl_pnl_summary, hl_trade_history, protocol_events, pool_snapshots)

---

### **SOLANA PARSING PIPELINE (END-TO-END)**

#### **1. Data Ingestion (solana.rs / solana_grpc.rs)**

- **RPC path**: `SolanaAdapter::fetch_history()` → calls `get_signatures_for_address_with_config()` → fetches full transaction JSON via `get_transaction(..., UiTransactionEncoding::Json)`
- **gRPC path**: `SolanaGrpcAdapter::stream_transactions()` → subscribes to `SubscribeRequestFilterTransactions` with program IDs (default: System, Token, AssocToken) → streams transactions in real-time with automatic reconnection on failure
- Both paths construct `Transaction` objects with:
  - `wallet_address` (lowercase by convention, but case-sensitive base58 by definition)
  - `tx_hash` (signature)
  - `raw_metadata` (full JSON from RPC/gRPC, includes `slot`, `transaction`, `meta`)
  - User and chain context

#### **2. Storage (repo.rs)**

- `save_transactions()` → chunks by 500 and batches into DB
- Insert query uses `ON CONFLICT (chain, tx_hash) DO NOTHING` (idempotent)
- Table `transactions` (V1) has columns: `id`, `user_id`, `wallet_address`, `timestamp`, `tx_hash`, `chain`, `raw_metadata`
- Supports V2 via `ensure_wallet_target()` which creates an `IndexTarget` if needed

#### **3. Parsing to Ledger Entries (solana_parser.rs)**

Entry point: `parse_solana_transaction(&tx) -> Vec<LedgerEntry>`

**Fee handling (lines 23-71)**:
```
- If meta.err is Some() → skip (failed tx, no entries)
- Find wallet in message.account_keys
- If wallet is at index 0 (fee payer) AND fee > 0:
  - Entry 1: Fee as EntryType::Fee, amount = -fee_lamports (negative)
  - Entry 2 (if transfer_lamports != 0): Transfer as EntryType::Transfer, amount = lamport_change + fee (net)
  - Why: balance_change includes fee. To separate: transfer = change + fee
- Else (non-fee-payer or index != 0):
  - Entry: Transfer with amount = lamport_change (entire balance change)
```

**Critical details**:
- Uses `i128` for lamport math to avoid truncation when `u64 > i64::MAX`
- `lamports_to_sol()` converts via BigDecimal (no floating-point loss): `lamports / 1_000_000_000`
- SPL token parsing: compares wallet to `post.owner` (OptionSerializer<String>), matches by account_index
- Token decimals and symbol lookup from mint address

**P3 Materializers (new datasets)**:
- `extract_solana_token_transfers()`: emits one TokenTransfer per non-zero token balance delta
- `extract_solana_native_balance_deltas()`: emits one NativeBalanceDelta per account with pre/post/delta SOL and `is_fee_payer` flag
- `extract_solana_decoded_events()`: extracts top-level and inner instructions (both Parsed and Compiled)

**Test coverage** (solana_parser_test.rs):
- Native SOL send with fee separation: 2 entries (fee + transfer)
- Native SOL receive (non-fee-payer): 1 entry (transfer only)
- Fee-only transaction: 1 entry (fee)
- Failed transaction: 0 entries (skipped)

---

### **WALLET FILTERING (END-TO-END)**

#### **1. Config Loading (config.rs)**

```rust
// AppConfig.allowed_wallets: Option<String> from env SPECTRAPLEX_ALLOWED_WALLETS
pub fn allowed_wallets_set(&self) -> Option<HashSet<String>> {
    self.allowed_wallets.as_ref().map(|s| {
        s.split(',')
         .map(|w| w.trim().to_lowercase())  // ← CASE LOWERCASING
         .filter(|w| !w.is_empty())
         .collect()
    })
}
```

**Key behavior**:
- Splits on comma, trims, converts to lowercase
- Empty values filtered out
- Returns `None` if `allowed_wallets` not set (no filtering)
- HashSet enables O(1) membership check

**Tests** (config.rs lines 102-129):
- `test_allowed_wallets_set_none`: None when not set
- `test_allowed_wallets_set_parses_csv`: "Wallet1, wallet2 ,WALLET3" → {wallet1, wallet2, wallet3}
- `test_allowed_wallets_set_ignores_empty`: "wallet1,,wallet2," → {wallet1, wallet2}

#### **2. API Enforcement (api/src/main.rs)**

```rust
fn check_wallet_allowed(wallet: &str, allowed: &Option<HashSet<String>>) -> Result<(), AppError> {
    if let Some(set) = allowed {
        if !set.contains(&wallet.to_lowercase()) {  // ← QUERY-TIME LOWERCASING
            return Err(AppError::forbidden("Wallet not in allowed set"));
        }
    }
    Ok(())
}
```

**Called at**:
- `trigger_ingest` (line 720)
- `trigger_batch_ingest` (line 915)
- `trigger_normalize` (line 955)
- `get_transactions` (line 1088)
- `get_ledger` (line 1106)
- `export_ledger` (line 1144)
- `get_single_transaction` (line 1208)
- `get_wallet_stats` (line 1229)
- `get_balances` (line 1248)

**State initialization** (main.rs lines 257-267):
```rust
let allowed_wallets = config.allowed_wallets_set();
let shared_state = Arc::new(AppState {
    allowed_wallets,
    // ...
});
```

#### **3. Query-Time Filtering (repo.rs)**

```sql
SELECT ... FROM transactions WHERE wallet_address = $1
SELECT ... FROM ledger_entries WHERE wallet_address = $1
SELECT ... FROM balances WHERE wallet_address = $1
```

**Critical issue**: Direct string comparison. If wallet stored as lowercase but query passes uppercase, no match.

#### **4. Address Normalization (v2.rs)**

```rust
pub fn normalize_evm_address(addr: &str) -> String {
    addr.to_lowercase()
}

pub fn normalize_solana_address(addr: &str) -> String {
    addr.to_string()  // Solana base58 is case-sensitive by definition
}
```

**Implication**: Solana wallets must be passed in exact case. Config's `to_lowercase()` is **incorrect for Solana** but the repo filter is case-sensitive (base58), so it's currently caught by accident.

---

### **TEST COVERAGE ANALYSIS**

**Strong areas**:
- Solana parser fee/transfer separation (4 unit tests)
- Config CSV parsing (3 unit tests)
- Materializer contracts and hashes (6 tests for distinct parser versions)
- Lamports-to-SOL conversion with overflow safety (2 tests)
- Decoded events for failed transactions (3 tests in P3-W3)

**Gaps**:
- No integration tests for API wallet filtering
- No end-to-end tests for Solana ingestion → parsing → storage
- No tests for EVM/Hyperliquid with wallet filtering
- No tests for address normalization across chains in actual filtering
- No tests for failed transactions in SPL token parsing (only for decoded events)
- No tests for token transfers with wallet mismatch

---

### **DATA INTEGRITY PATHS**

1. **Solana transaction → raw_transactions (V2)**
   - `SolanaAdapter::backfill()` → builds `RawTransaction` (no wallet/user)
   - Inserted to `raw_transactions` (idempotent on network+tx_hash)
   - Matches to target via `target_matches` table

2. **Solana transaction → transactions (V1, legacy)**
   - Ingestion fills wallet_address directly
   - Stored with user_id
   - Query filters by wallet_address

3. **Failed transactions**
   - Skipped entirely: `if meta.err.is_some() { return Ok(vec![]) }`
   - No ledger entries created
   - Raw transaction still stored (Bronze layer)

4. **Wallet filtering**
   - Config: env var → HashSet (lowercase for all)
   - API: check_wallet_allowed at every endpoint
   - Repo: query by wallet_address (case-sensitive string match)
   - **Gap**: Solana addresses case-lowercased at config time but compared case-sensitively at query time

5. **Fee extraction**
   - Solana: meta.fee (u64) always available
   - EVM: gas_used × effective_gas_price (from metadata hex fields)
   - Stored as EntryType::Fee with negative amount

---

### **WHAT TO LOOK FOR IN A PR FIXING SOLANA PARSER / WALLET FILTERING BUGS**

#### **Solana Parser Bugs**

1. **Fee separation logic**: Does it correctly handle:
   - Fee-payer at index 0? Yes (is_fee_payer = idx == 0)
   - Fee-only transactions (transfer_lamports = 0)? Yes (skips transfer entry)
   - Non-fee-payer balance changes? Yes (all balance change is transfer)
   - Zero fees? Yes (doesn't create Fee entry if fee == 0)

2. **Token transfer edge cases**:
   - owner field missing (OptionSerializer::None)? Returns 0 entries (correct)
   - account_index mismatch in pre/post? Falls back to 0 (correct)
   - Malformed amount string? Unwraps to 0 (silent failure—possible bug)

3. **Precision**:
   - i128 usage in lamport change calculation? ✓
   - BigDecimal for conversions? ✓
   - Token decimals applied correctly? Need to check individual tokens

4. **Failed transaction handling**:
   - meta.err check at start? ✓
   - Skips all entries including fees? **Potential bug**: should fees be charged even on failed txs? (Currently skipped)

#### **Wallet Filtering Bugs**

1. **Case sensitivity**:
   - Is `allowed_wallets_set()` applied consistently to all chains?
   - For Solana (case-sensitive base58): lowercasing in config + case-sensitive query = **bug**
   - For EVM (hex, case-insensitive): lowercasing everywhere = **correct**

2. **Checkpoint handling**:
   - When resuming with checkpoint, does it preserve wallet address casing?
   - Does checkpoint comparison work with different cases?

3. **API enforcement**:
   - Is check_wallet_allowed called on ALL endpoints that return wallet data?
   - Is it called BEFORE database access (not after)?
   - Does it handle None (no filter) correctly?

4. **Database consistency**:
   - Are wallet addresses stored in consistent case?
   - Legacy V1: whatever was ingested (may be mixed case)
   - V2: matches raw_transactions → target_matches → target.address

#### **Red Flags in a PR**

- Changes to `parse_solana_transaction()` without updating test cases
- Modifies fee logic without explanation of Solana fee semantics
- Stores wallet addresses in normalized (lowercase) form for Solana
- Adds filtering that doesn't work with case-sensitive base58 addresses
- Removes failed transaction checks
- Changes BigDecimal math to floating-point
- Modifies `to_lowercase()` in config without understanding chain-specific normalization

---

### **RECENT COMMITS AND CURRENT STATE**

Latest commits (git log):
- `8b6810c` Merge #134: README and docs refresh (just done)
- `b91ee84` Stop tracking handoff docs
- `d06b9e3` Merge P5-W3: Protocol/TVL analytics
- `15c4771` Add protocol/TVL analytics pack
- `b87a9f2` Merge P5-W2: Hyperliquid analytics
- `7fa6790` Add Hyperliquid analytics pack
- `0279019` Merge P5-W1: Wallet/Tax/Forensics pack

**Current state**: Mature multi-phase release cycle (P5-W3 complete). Strong focus on Gold datasets and analytics. Wallet/tax path is stable. V2 target-centric architecture is active.

---

### **KEY FILES FOR REVIEW**

**Critical files for PR analysis**:

1. `/adapters/src/solana_parser.rs` (lines 1-176): Core fee/transfer logic
2. `/core/src/config.rs` (lines 60-67): allowed_wallets_set() CSV parsing
3. `/api/src/main.rs` (lines 706-713): check_wallet_allowed() enforcement
4. `/core/src/v2.rs` (lines 357-365): Address normalization
5. `/adapters/src/repo.rs` (lines 224-273): Query filtering
6. `/adapters/tests/solana_parser_test.rs`: Parser test cases

**Secondary files for context**:
- `/migrations/20251219000000_init.sql`: V1 schema
- `/migrations/20260308000002_add_v2_tables.sql`: V2 schema
- `/cli/src/main.rs`: Ingestion entry point

---

## SUMMARY

I am now prepared to provide **rigorous, expert-level PR reviews** for Spectraplex. I understand:

1. **How Solana parsing works end-to-end**: From RPC/gRPC ingestion through fee separation to ledger entry creation, with special attention to i128 math, failed transaction handling, and SPL token accuracy.

2. **How wallet filtering works end-to-end**: From CSV config parsing (with problematic lowercase normalization), through API enforcement at every endpoint, to case-sensitive database queries that conflict with Solana's base58 semantics.

3. **What test coverage exists**: Strong unit tests for core logic (fee separation, config parsing, materializer contracts) but gaps in integration and cross-chain scenarios.

4. **What red flags to watch for**: Wallet case handling bugs, fee logic changes, BigDecimal precision issues, skipped failed transaction handling, and missing API filter enforcement.

5. **The product direction**: Target-centric indexing where wallets are first-class but not the organizing principle; careful to preserve generalization across eight target kinds while maintaining backward compatibility.

I am ready to be resumed with specific PRs to review.</result>
<usage><total_tokens>84220</total_tokens><tool_uses>25</tool_uses><duration_ms>163594</duration_ms></usage>
</task-notification>
Full transcript available at: REDACTED.output

---

<task-notification>
<task-id>a5e71659b8c9c2591</task-id>
<tool-use-id>toolu_01P5Pz8ShsTAvschYoG38VXn</tool-use-id>
<output-file>REDACTED.output</output-file>
<status>completed</status>
<summary>Agent "Implement security fixes #140 #136" completed</summary>
<result>The PR has been created: **https://github.com/user1303836/spectraplex/pull/160**

Here is a summary of what was done:

### Issue #140: SSRF IPv6 bypass
**File:** `/Users/user1303836/Development/spectraplex/.claude/worktrees/agent-a5e71659/api/src/main.rs` (line ~520)

The `is_private_ip()` function's IPv6 branch only checked `is_loopback()` and `is_unspecified()`. Added checks for:
- **ULA** (`fc00::/7`) via `(seg0 & 0xfe00) == 0xfc00`
- **Link-local** (`fe80::/10`) via `(seg0 & 0xffc0) == 0xfe80`
- **Multicast** (`ff00::/8`) via `(seg0 & 0xff00) == 0xff00`
- **IPv4-mapped IPv6** (`::ffff:0:0/96`) -- delegates to the same IPv4 private checks using `to_ipv4_mapped()`

Added 5 new unit tests covering each IPv6 range plus public IPv6 addresses.

### Issue #136: Content-Disposition header injection
**File:** same file, `download_export()` function (line ~2843)

The `Content-Disposition` header was built by interpolating `entry.status.dataset` and `entry.status.format` directly into the header value. Added a `sanitize` closure that strips all characters except `[a-zA-Z0-9_-]` and applied it to both values before header construction. While upstream validation (dataset whitelist, `ExportFormat` enum parsing) currently constrains these to safe values, this provides defense in depth against future changes.

Added 1 new unit test that injects CRLF and HTML into both fields and verifies they are stripped from the resulting header.

### Verification
- `cargo fmt --all --check` -- clean
- `cargo clippy --workspace --all-targets -- -D warnings` -- clean
- `cargo test --workspace` -- 292 passed, 1 pre-existing failure (`test_semaphore_limits_concurrent_jobs`, confirmed unrelated)</result>
<usage><total_tokens>65139</total_tokens><tool_uses>66</tool_uses><duration_ms>484173</duration_ms></usage>
<worktree><worktreePath>/Users/user1303836/Development/spectraplex/.claude/worktrees/agent-a5e71659</worktreePath><worktreeBranch>worktree-agent-a5e71659</worktreeBranch></worktree>
</task-notification>
Full transcript available at: REDACTED.output

---

<task-notification>
<task-id>a82118ddb0dbff690</task-id>
<tool-use-id>REDACTED</tool-use-id>
<output-file>REDACTED.output</output-file>
<status>completed</status>
<summary>Agent "Implement core bugs #145 #143" completed</summary>
<result>The PR has been created: **https://github.com/user1303836/spectraplex/pull/161**

Here is a summary of what was done:

### Issue #145: Solana parser skips failed transactions entirely, losing fee entries
**Files changed:**
- `/adapters/src/solana_parser.rs` — In `parse_solana_transaction`, the early `return Ok(vec![])` for `meta.err.is_some()` was removed. The fee entry is now always emitted for the fee payer regardless of transaction success. Transfer and SPL token extraction are skipped for failed transactions since those effects do not occur. In `extract_solana_native_balance_deltas`, the `meta.err.is_some()` guard was removed entirely so balance deltas (including fee deductions) are captured for failed transactions. `extract_solana_token_transfers` and `extract_solana_decoded_events` retain their skip logic since token transfers and instruction effects do not occur on failed transactions.
- `/adapters/tests/solana_parser_test.rs` — The integration test `test_failed_transaction_skipped` was renamed to `test_failed_transaction_emits_fee` and updated to assert exactly one fee entry is produced.
- Three new unit tests were added in `solana_parser.rs`: `test_parse_failed_tx_emits_fee_entry`, `test_extract_native_balance_deltas_failed_tx`, and `test_extract_token_transfers_failed_tx_skip`.

### Issue #143: `allowed_wallets` lowercases Solana base58 addresses, breaking filtering
**Files changed:**
- `/core/src/config.rs` — `allowed_wallets_set()` now only lowercases addresses with a `0x`/`0X` prefix (EVM). All other address formats are preserved as-is. The existing test was replaced with `test_allowed_wallets_set_lowercases_evm_only` that validates both behaviors.
- `/api/src/main.rs` — `check_wallet_allowed()` applies the same chain-aware normalization: only `0x`-prefixed addresses are lowercased before lookup. Tests `test_check_wallet_allowed_evm_case_insensitive` and `test_check_wallet_allowed_solana_case_sensitive` were added.

### Pre-existing issue noted
`test_semaphore_limits_concurrent_jobs` in the API crate fails on clean `main` as well -- it is unrelated to these changes.</result>
<usage><total_tokens>122652</total_tokens><tool_uses>81</tool_uses><duration_ms>780716</duration_ms></usage>
<worktree><worktreePath>/Users/user1303836/Development/spectraplex/.claude/worktrees/agent-a82118dd</worktreePath><worktreeBranch>worktree-agent-a82118dd</worktreeBranch></worktree>
</task-notification>
Full transcript available at: REDACTED.output

---

<task-notification>
<task-id>ab96e5305633d679d</task-id>
<tool-use-id>toolu_016aD81TfxWVsKspcex6Eshd</tool-use-id>
<output-file>REDACTED.output</output-file>
<status>completed</status>
<summary>Agent "Review security PR #160" completed</summary>
<result>The review has been submitted. Here is a summary of the findings:

---

**PR #160 Review Summary**

**Content-Disposition Fix (Issue #136):** Correct and complete. The `[a-zA-Z0-9_-]` sanitization filter properly prevents header injection. Well-tested. No issues.

**SSRF IPv6 Fix (Issue #140):** Has one remaining bypass that must be fixed before merge.

**Critical finding:** The PR uses `v6.to_ipv4_mapped()` which only catches `::ffff:x.x.x.x` (IPv4-mapped) addresses. It misses IPv4-compatible addresses like `::10.0.0.1` (which normalizes to `::a00:1`). An attacker can use `http://[::10.0.0.1]/` to reach private IPv4 addresses, completely bypassing the SSRF protection.

**Required fix:** Change `to_ipv4_mapped()` to `to_ipv4()` on line 542 of `/Users/user1303836/Development/spectraplex/.claude/worktrees/agent-a5e71659/api/src/main.rs`. The `to_ipv4()` method covers both IPv4-mapped AND IPv4-compatible addresses. Add test cases for `::10.0.0.1`, `::127.0.0.1`, and `::8.8.8.8`.

**Lower-priority gaps noted for follow-up:** 6to4 addresses (`2002::/16`) and Teredo addresses (`2001:0000::/32`) embedding private IPv4 are not blocked, but these are low severity given the protocols' declining use.

**Style note (non-blocking):** The manual bitmask checks are correct but could use the now-stable std methods (`is_unique_local()`, `is_unicast_link_local()`, `is_multicast()`) for better readability.</result>
<usage><total_tokens>57487</total_tokens><tool_uses>57</tool_uses><duration_ms>451166</duration_ms></usage>
</task-notification>
Full transcript available at: REDACTED.output

---

<task-notification>
<task-id>b65cw0go2</task-id>
<tool-use-id>REDACTED</tool-use-id>
<output-file>REDACTED.output</output-file>
<status>killed</status>
<summary>Background command "Verify bitmask math for IPv6 range checks" was stopped</summary>
</task-notification>
Read the output file to retrieve the result: REDACTED.output

---

<task-notification>
<task-id>af67ac2998097603d</task-id>
<tool-use-id>REDACTED</tool-use-id>
<output-file>REDACTED.output</output-file>
<status>completed</status>
<summary>Agent "Review core bugs PR #161" completed</summary>
<result>The review has been submitted. Here is a summary of the findings:

## Review Summary for PR #161

**Files reviewed:**
- `/Users/user1303836/Development/spectraplex/.claude/worktrees/agent-a82118dd/adapters/src/solana_parser.rs` -- core Solana parser changes
- `/Users/user1303836/Development/spectraplex/.claude/worktrees/agent-a82118dd/adapters/tests/solana_parser_test.rs` -- integration tests
- `/Users/user1303836/Development/spectraplex/.claude/worktrees/agent-a82118dd/core/src/config.rs` -- wallet filtering config
- `/Users/user1303836/Development/spectraplex/.claude/worktrees/agent-a82118dd/api/src/main.rs` -- runtime wallet check
- `/Users/user1303836/Development/spectraplex/.claude/worktrees/agent-a82118dd/adapters/src/ledger_derivation.rs` -- related code with the same bug

**Key findings:**

1. **Both core fixes are logically correct.** The failed-tx fee extraction and the base58 case-sensitivity fix are sound.

2. **Critical: `adapters/src/ledger_derivation.rs` has the same case-sensitivity bug** in four functions that unconditionally lowercase wallet addresses when comparing. This is the Silver-to-Ledger derivation path, meaning it affects the V2 data pipeline. This is the same class of bug as #143 and should be fixed in this PR or explicitly tracked.

3. **Missing test coverage:** No test for a failed tx where the tracked wallet is NOT the fee payer (should produce zero entries), and no test for `extract_solana_native_balance_deltas` with multiple accounts on a failed tx.

4. **Minor: dead `let _ = entry_index;`** on line 97 of `solana_parser.rs` serves no purpose.</result>
<usage><total_tokens>72698</total_tokens><tool_uses>26</tool_uses><duration_ms>158911</duration_ms></usage>
</task-notification>
Full transcript available at: REDACTED.output

---

Yep. And have them continue making PRs for medium sev issues as well. If all PR comments are addressed, they can override and merge.

---

<task-notification>
<task-id>a1b4208df4dc19ae8</task-id>
<tool-use-id>REDACTED</tool-use-id>
<output-file>REDACTED.output</output-file>
<status>completed</status>
<summary>Agent "Fix PR #160 reviews + medium issues" completed</summary>
<result>Done. Here is the summary:

**PR #160 (Job 1):** Already merged at commit `8537b28`. No action needed.

**PR #162 (Job 2):** Created at https://github.com/user1303836/spectraplex/pull/162 on branch `fix/medium-issues-149-150-153-160`. It addresses four issues:

1. **#140 (SSRF follow-up):** Changed `to_ipv4_mapped()` to `to_ipv4()` in `is_private_ip()` to block IPv4-compatible addresses (`::10.0.0.1`, `::127.0.0.1`). Replaced manual bitmask checks with stable std methods. Added `test_is_private_ip_ipv4_compatible_ipv6` test.

2. **#153:** Changed `create_export_job()` return type to `Result<(StatusCode, Json<ExportJobStatus>), AppError>` and returns `202 Accepted` instead of `200 OK`. Updated 13 export tests.

3. **#150:** Changed `require_auth()` to return `500 Internal Server Error` (not `401 Unauthorized`) when `SPECTRAPLEX_API_KEY` is not configured.

4. **#149:** Added migration `migrations/20260313000000_add_ledger_entries_transaction_id_index.sql` creating `idx_ledger_entries_transaction_id` on `ledger_entries(transaction_id)`.

**Checks:** `cargo fmt`, `cargo clippy`, and `cargo test` all pass. The one failing test (`test_semaphore_limits_concurrent_jobs`) is a pre-existing failure on `main`.

**Files modified:**
- `/Users/user1303836/Development/spectraplex/api/src/main.rs` (32 insertions, 27 deletions)
- `/Users/user1303836/Development/spectraplex/migrations/20260313000000_add_ledger_entries_transaction_id_index.sql` (new)

**Not merged** per instructions.</result>
<usage><total_tokens>100295</total_tokens><tool_uses>465</tool_uses><duration_ms>2542637</duration_ms></usage>
</task-notification>
Full transcript available at: REDACTED.output

---

<task-notification>
<task-id>a5d24fedfc09f420c</task-id>
<tool-use-id>toolu_01Rw3F2phu7pwpMf7JWS2xLg</tool-use-id>
<output-file>REDACTED.output</output-file>
<status>completed</status>
<summary>Agent "Fix PR #161 reviews + medium issues" completed</summary>
<result>PR created. Here is the summary of everything done:

**Job 2 completed.** Branch `fix/medium-issues-152-146-148` created from latest `main` with all three medium issues implemented:

1. **Issue #152 — Config validation** (`/Users/user1303836/Development/spectraplex/core/src/config.rs`):
   - Added `ConfigError` enum with `Figment` and `Validation` variants
   - Added `validate()` method checking: `database_url` not empty/whitespace, `port` non-zero, `pool_size` between 1 and 1000
   - Added 8 unit tests covering all validation paths and boundaries

2. **Issue #146 — Enum CHECK constraints** (`REDACTED.sql`):
   - `ingestion_runs.status` IN ('pending', 'running', 'completed', 'failed', 'cancelled')
   - `ingestion_runs.mode` IN ('backfill', 'stream', 'both')
   - `dataset_versions.status` IN ('active', 'superseded', 'failed')
   - `wallet_ledger.entry_type` IN ('trade', 'fee', 'transfer', 'staking', 'income', 'funding')
   - `networks.finality_model` IN ('probabilistic-slot', 'probabilistic-block', 'deterministic-block', 'instant')
   - `hl_fills.side` and `hl_fill_records.side` IN ('B', 'A')

3. **Issue #148 — API rate limiting** (`/Users/user1303836/Development/spectraplex/api/src/main.rs`):
   - `RateLimiter` struct with `TokioMutex<HashMap<String, TokenBucket>>`, 60 req capacity, 10 tokens/sec refill
   - `rate_limit_middleware` extracts Bearer token as bucket key, returns 429 when exhausted
   - Applied as layer on all `/v1/*` routes, running after auth
   - All 4 `AppState` constructions (1 production + 3 test) updated with `rate_limiter` field

All checks pass: `cargo fmt`, `cargo clippy`, `cargo test` (939 tests, 0 failures).

PR: https://github.com/user1303836/spectraplex/pull/163 (not merged, as instructed).</result>
<usage><total_tokens>131853</total_tokens><tool_uses>404</tool_uses><duration_ms>2595064</duration_ms></usage>
</task-notification>
Full transcript available at: REDACTED.output

---

<task-notification>
<task-id>ae68397f9d8e78acc</task-id>
<tool-use-id>toolu_012XeWFSBK7pN8oQYQ3ES1ve</tool-use-id>
<output-file>REDACTED.output</output-file>
<status>completed</status>
<summary>Agent "Review medium issues PR #163" completed</summary>
<result>The review has been submitted. Here is a summary of the findings:

## Review Results for PR #163

**8 issues identified across 3 fixes, 2 high-severity.**

### High Severity
1. **`validate()` is dead code** (`/Users/user1303836/Development/spectraplex/core/src/config.rs`): The method is defined with good tests but never called in `api/src/main.rs` (line 294) or `cli/src/main.rs` after `AppConfig::load()`. Invalid configs will proceed unchecked to DB connection.

2. **Unbounded HashMap in RateLimiter** (`/Users/user1303836/Development/spectraplex/api/src/main.rs`, lines 121-163): The `buckets` HashMap grows without bound. Every unique Bearer token creates a permanent entry with no eviction, creating a memory leak vector.

### Medium Severity
3. **Missing `hl_position_changes.side` CHECK** (`REDACTED.sql`): The migration covers `hl_fills.side` and `hl_fill_records.side` but omits `hl_position_changes.side`, which has the same "B"/"A" domain.

4. **Migration is not idempotent**: Uses bare `ADD CONSTRAINT` which will fail if constraints already exist. Needs `DROP CONSTRAINT IF EXISTS` or `DO $$` guards.

5. **No rate limiting tests**: The RateLimiter is wired into test setup but its behavior (bucket exhaustion, refill, per-key isolation) is never exercised.

### Confirmed Correct
- Middleware ordering (auth before rate limit) is correct
- `wallet_ledger.entry_type` CHECK including `'funding'` is correct (used in `ledger_derivation.rs`)
- `FinalityModel` CHECK values match the Rust enum exactly
- Rate limit defaults (60 capacity / 10 tokens/sec) are reasonable
- Config validation logic itself (rules, edge cases, tests) is well-implemented</result>
<usage><total_tokens>72876</total_tokens><tool_uses>44</tool_uses><duration_ms>185693</duration_ms></usage>
</task-notification>
Full transcript available at: REDACTED.output

---

<task-notification>
<task-id>a0911867eabbacfe3</task-id>
<tool-use-id>toolu_01Ed6RjRfJMaQJDh1zxRkKJm</tool-use-id>
<output-file>REDACTED.output</output-file>
<status>completed</status>
<summary>Agent "Review medium issues PR #162" completed</summary>
<result>The review has been submitted. Here is a summary of the findings:

**PR #162 Review Summary**

Three of the four fixes are correct and ready to merge. One has a blocking issue:

1. **SSRF IPv4-compatible bypass (#140 follow-up)** -- Correct. Switching `to_ipv4_mapped()` to `to_ipv4()` closes a real bypass where IPv4-compatible addresses like `::10.0.0.1` could evade SSRF protection. The std method replacements (`is_unique_local`, `is_unicast_link_local`, `is_multicast`) are all stable on the project's Rust 1.92.0 and semantically equivalent to the manual bitmask checks they replace. The `::1` edge case with `to_ipv4()` is safe because `v6.is_loopback()` catches it earlier in the chain.

2. **Export 202 Accepted (#153)** -- Correct. Applied only to the async `create_export_job` path. Synchronous exports (`export_ledger`, `tax_export`) correctly remain 200 OK. All 12 affected tests updated.

3. **Auth 500 for missing key (#150)** -- Correct. Properly distinguishes server misconfiguration (500) from client auth failure (401). No information leakage concern.

4. **Ledger entries index (#149)** -- **Blocking issue.** The migration file `20260313000000_add_ledger_entries_transaction_id_index.sql` sorts BEFORE the existing `20260313100000_add_enum_check_constraints.sql`. The sqlx migrator will error on any database that already has `20260313100000` applied. The fix is to rename it to `20260313200000_add_ledger_entries_transaction_id_index.sql` (or any timestamp after `20260313100000`).</result>
<usage><total_tokens>71201</total_tokens><tool_uses>57</tool_uses><duration_ms>264014</duration_ms></usage>
</task-notification>
Full transcript available at: REDACTED.output

---

<task-notification>
<task-id>a36ac48ef41c4002c</task-id>
<tool-use-id>REDACTED</tool-use-id>
<output-file>REDACTED.output</output-file>
<status>completed</status>
<summary>Agent "Fix PR #162 review comments + merge" completed</summary>
<result>PR #162 has been successfully merged. Here is a summary of what was done:

**Review comment addressed:**
- Renamed migration file from `migrations/20260313000000_add_ledger_entries_transaction_id_index.sql` to `migrations/20260313200000_add_ledger_entries_transaction_id_index.sql` so it sorts after the existing `20260313100000_add_enum_check_constraints.sql` migration. This prevents sqlx migrator `VersionMissing` errors on databases that already have the enum constraints migration applied.

**Verification:**
- `cargo fmt --all --check` -- passed
- `cargo clippy --workspace --all-targets -- -D warnings` -- passed
- `cargo test --workspace` -- all tests passed (locally and in CI)
- All 4 GHA CI jobs passed (Formatting, Clippy, Tests, Build)

**PR #162** merged via squash at commit `4235e08` on 2026-03-13.</result>
<usage><total_tokens>27815</total_tokens><tool_uses>31</tool_uses><duration_ms>241758</duration_ms></usage>
</task-notification>
Full transcript available at: REDACTED.output